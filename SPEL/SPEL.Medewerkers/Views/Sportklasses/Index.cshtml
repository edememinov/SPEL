@model IEnumerable<SPEL.Domain.Entities.Sportklasse>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.sport.naam)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.naam)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.minLeeftijd)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.maxLeeftijd)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.indeling)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.sport.naam)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.naam)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.minLeeftijd)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.maxLeeftijd)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.indeling)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
